server:
  port: 8082
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
  application:
    name: people-service
  datasource:
    url: jdbc:h2:mem:testdb
    username: myuser
    password: mypassword
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update